# 教培机构物资管理系统 — 需求规格说明书

---

## 一、系统定位

总部管理员采购物资 → 配发给各校区活动 → 按不同维度计算摊销成本。

一条主线，三个环节。轻量级、单机部署，跳过传统出入库流程，以活动为驱动进行物资消耗与成本核算。

---

## 二、校区管理

校区只保留三个字段：

| 字段 | 说明 |
|------|------|
| 名称 | 必填 |
| 类型 | 总部 / 普通校区（总部节点系统内置，不可删除） |
| 创建时间 | 系统自动记录 |

---

## 三、账户与角色

| 角色 | 职责 | 数据范围 |
|------|------|---------|
| **总部管理员** | 采购物资、配发物资、管理校区和账户、查看全平台报表、历史修正 | 全部数据 |
| **校区操作员** | 创建和管理本校区活动、查看本校区报表 | 仅本校区 |
| **活动联系人** | 录入关联活动的执行次数、触发结算回收 | 仅关联活动 |

**规则**：
- 所有账户由总部管理员统一创建与管理，不支持自助注册。
- 总部管理员绑定总部校区；校区操作员和活动联系人绑定指定校区。
- 密码由管理员设置，不做复杂度要求。
- 账户支持一键禁用，禁用后即时失效。
- 登录认证使用 JWT，有效期 1 天。

---

## 四、核心业务主流程

### 4.1 采购入库

总部管理员录入采购单，物资进入总部库存池。

```
选择物资分类 → 填写物资名称 → 填写数量、总金额 → 提交
→ 系统自动计算单价（总金额 ÷ 数量）
→ 物资进入总部库存
```

**业务规则**：
- 一个采购单只含一种物资。
- 采购不绑定活动，总部可自由采购入库。
- 数量、金额需做前端和后端双重校验，禁止负数和零值。
- 计量单位不强制填写。

### 4.2 配发到活动

总部管理员将总部库存中的物资配发给指定活动。

```
选择库存物资 → 选择目标活动 → 输入配发数量 → 提交
→ 系统校验：该物资累计配发量 ≤ 库存余量
→ 通过 → 扣减总部库存，物资记入活动
→ 不通过 → 拦截并提示"库存不足"
```

**业务规则**：
- 一种物资可配发给多个活动，每次单独操作。
- 配发总量受库存余量硬约束，不可超发。
- 支持对已配发物资进行追加配发或调减退回，退回后物资回归总部库存。
- 所有配发调整均需记录操作人和原因。

### 4.3 活动管理

校区操作员或总部管理员创建活动：

| 字段 | 说明 |
|------|------|
| 活动名称 | 必填 |
| 所属校区 | 必选 |
| 活动联系人 | 可多选，从该校区账户中选取 |
| 计划执行次数 | 必填，正整数 |
| 开始日期 | 必填 |
| 结束日期 | 必填 |

**活动状态流转**：

```
未开始 ──→ 进行中 ──→ 已结束 ──→ 已结算 ──→ 已归档
               ↑                      │
               ←────── 回撤结算 ←──────
```

- 到达开始日期自动变为「进行中」，到达结束日期自动变为「已结束」。
- 已结束的活动可手动结算，结算后可回撤，回撤后恢复为「已结束」。
- 已结算且确认不再变动后可手动归档。

**执行次数录入**：
- 仅活动联系人或总部管理员可录入或修改。
- 校验规则：须为正整数、不超过计划执行次数。
- 历史执行次数允许回溯修正，修正后触发摊销重算。

### 4.4 结算回收

活动结束后，由活动联系人或总部管理员手动触发结算回收。

```
点击"结算回收" → 弹出预览窗口
→ 展示：待回收物资清单、回收数量、成本扣减金额
→ 确认执行：
    1. 剩余物资回归总部库存
    2. 扣减活动摊销基数
    3. 重算受影响摊销快照
    4. 记录审计日志
```

**业务规则**：
- 仅手动触发，不支持自动结算。
- 结算前须预览确认，确认后方可执行。
- 若已使用数量大于配发数量（异常），系统拦截并禁止生成回收记录。
- 结算支持回撤：回撤后物资从总部库存扣回，摊销基数恢复，活动状态回到「已结束」。
- 所有结算和回撤操作均须记录审计日志。

---

## 五、成本摊销计算

### 5.1 核心公式

```
活动摊销成本基数 = 该活动配发物资总金额 - 已回收物资金额

活动累计摊销成本 = 摊销成本基数 × (累计执行次数 ÷ 计划总次数)

每日摊销金额 = 摊销成本基数 × (当日执行次数 ÷ 计划总次数)
```

### 5.2 摊销快照

- 以天为最小粒度生成摊销快照，每天一条记录。
- 每日记录：该活动的执行次数、有效摊销基数、当日摊销金额。

### 5.3 历史修正联动

当修改历史执行次数或配发数量时，系统在**同一事务内**完成：

1. 更新目标数据
2. 定位受影响日期范围
3. 重算对应日期的摊销快照
4. 更新活动总摊销统计
5. 记录审计日志（操作人、变更前后值、影响金额）

### 5.4 统计维度

| 维度 | 说明 |
|------|------|
| 按活动 | 单个活动全周期的物资投入与摊销总额 |
| 按日期范围 | 指定时间段内所有活动的日摊销汇总 |
| 按校区 | 指定校区下所有活动的成本汇总 |
| 按物资品类 | 跨活动统计同类物资的消耗总量与总金额 |

---

## 六、物资分类

物资须关联分类，采购时选择。系统预设常见分类（如教材、文具、设备、耗材等），支持总部管理员增删改。

分类用于：
- 采购时的物资归类
- 「按物资品类」维度的摊销统计报表

---

## 七、审计日志

以下操作必须记录审计日志：

- 采购入库
- 物资配发与调整
- 执行次数修正
- 结算回收与回撤
- 任何涉及金额变动的操作

每条日志包含：

| 字段 | 说明 |
|------|------|
| 操作人 | 账户ID |
| 操作时间 | 精确到秒 |
| 操作类型 | 采购/配发/调拨/结算/回撤/修正等 |
| 操作对象 | 关联的业务记录 |
| 变更前值 | 原始数据 |
| 变更后值 | 新数据 |
| 影响金额 | 涉及金额时必填 |

---

## 八、输入校验规则

所有数值型输入须做前端和后端双重校验：

| 场景 | 校验规则 |
|------|---------|
| 采购数量 | 正整数，> 0 |
| 采购总金额 | 正数，> 0 |
| 计划执行次数 | 正整数，> 0 |
| 执行次数录入 | 正整数，> 0，≤ 计划总次数 |
| 配发数量 | 正整数，> 0，累计配发量 ≤ 库存余量 |
| 结算回收量 | 正整数，≤ 活动当前持有量 |
| 全部场景 | 禁止负数、禁止零值（备注除外）、禁止异常超大值 |

---

## 九、权限矩阵

| 操作 | 总部管理员 | 校区操作员 | 活动联系人 |
|------|:---:|:---:|:---:|
| 校区管理 | ✅ | — | — |
| 账户管理 | ✅ | — | — |
| 物资采购 | ✅ | — | — |
| 物资配发 | ✅ | — | — |
| 创建活动 | ✅ | ✅（本校区） | — |
| 录入执行次数 | ✅ | — | ✅（关联活动） |
| 结算回收 | ✅ | — | ✅（关联活动） |
| 回撤结算 | ✅ | — | ✅（关联活动） |
| 历史修正 | ✅ | — | — |
| 全局报表 | ✅ | — | — |
| 本校区报表 | — | ✅ | — |
| 活动看板 | ✅ | ✅（本校区） | ✅（关联活动） |

---

## 十、非功能性需求

| 类别 | 要求 |
|------|------|
| 部署形态 | 单机部署，无需安装数据库服务端，启动即用 |
| 数据库 | SQLite |
| 认证方式 | JWT Token，有效期 1 天 |
| 性能-修正重算 | 单次 ≤ 500ms |
| 性能-报表查询 | 日粒度查询 ≤ 1s（万级数据量） |
| 操作系统 | Windows |
| 浏览器 | Chrome、Edge、Firefox 最新两个主版本 |
| 数据备份 | SQLite 文件支持定期备份 |
| 配置安全 | 敏感配置通过环境变量注入 |
